<% seed_sentences.each do |seed_sentence| %>
  <%= form_with url: search_breed_index_path, id: 'js-seed-sentence-button-to-seeding' do |f| %>
    <%= f.submit seed_sentence.sentence, class: 'btn btn-primary' %>
    <%= f.hidden_field :sentence_id, :value => seed_sentence.sentence_id %>
  <% end %>
  <br>
  <br>
<% end %>