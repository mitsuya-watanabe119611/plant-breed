<% flower_sentences.each do |flower_sentence| %>
  <%= form_with url: search_breed_index_path, id: 'js-seed-sentence-button-to-seeding' do |f| %>
    <%= f.submit flower_sentence.sentence, class: 'btn btn-primary' %>
    <%= f.hidden_field :sentence_id, :value => flower_sentence.sentence_id %>
  <% end %>
  <br>
  <br>
<% end %>
